name: Building for b9

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: alpine-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: make
      run: make

    - uses: actions/upload-artifact@v4
      with:
        name: out
        path: ./
